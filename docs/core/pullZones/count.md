# `$bunny->core->pullZones->count()`

> Core Platform API · `GET /pullzone/count`

Count Pull Zones

## Signature

```php
public function count(): int
```

## Returns

`int`

## Example

```php
use GoSuccess\Bunny\Bunny;

$bunny = new Bunny('your-api-key');

$result = $bunny->core->pullZones->count();
```
