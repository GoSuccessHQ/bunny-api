<?php

declare(strict_types=1);

// This file is generated by tools/generate.php from resources/specs/core.json. Do not edit.

namespace GoSuccess\Bunny\Core\Resource;

use DateTimeInterface;
use GoSuccess\Bunny\Core\Enum\LogOrdering;
use GoSuccess\Bunny\Core\Model\AuditLogEntry;
use GoSuccess\Bunny\Core\Pagination;
use GoSuccess\Bunny\Http\Method;
use GoSuccess\Bunny\Model\Cast;
use GoSuccess\Bunny\Pagination\Page;
use GoSuccess\Bunny\Pagination\Paginator;
use GoSuccess\Bunny\Resource\AbstractResource;

/** The audit log of changes made to the account. */
final class AuditLogResource extends AbstractResource
{
    /**
     * `GET /user/audit/{date}`
     *
     * @param DateTimeInterface $date
     * @param list<string>|null $product
     * @param list<string>|null $resourceType
     * @param list<string>|null $resourceId
     * @param list<string>|null $actorId
     * @param LogOrdering|null  $order
     * @param string|null       $continuationToken The position returned by the previous page; null for the first page.
     * @param int|null          $limit             The number of items per page.
     *
     * @return Page<AuditLogEntry>
     */
    public function list(
        DateTimeInterface $date,
        ?array $product = null,
        ?array $resourceType = null,
        ?array $resourceId = null,
        ?array $actorId = null,
        ?LogOrdering $order = null,
        ?string $continuationToken = null,
        ?int $limit = null,
    ): Page {
        $data = self::expectObject($this->connection->json(Method::Get, "user/audit/{$this->segment($date)}", [
            'Product' => $product,
            'ResourceType' => $resourceType,
            'ResourceId' => $resourceId,
            'ActorId' => $actorId,
            'Order' => $order,
            'ContinuationToken' => $continuationToken,
            'Limit' => $limit,
        ]));

        return Pagination::continuation($data, Cast::modelList(AuditLogEntry::class, $data['Logs'] ?? null));
    }

    /**
     * Iterate lazily over every item of list(), across all pages.
     *
     * `GET /user/audit/{date}`
     *
     * @param DateTimeInterface $date
     * @param list<string>|null $product
     * @param list<string>|null $resourceType
     * @param list<string>|null $resourceId
     * @param list<string>|null $actorId
     * @param LogOrdering|null  $order
     * @param int|null          $limit        The number of items per page.
     *
     * @return Paginator<AuditLogEntry>
     */
    public function all(
        DateTimeInterface $date,
        ?array $product = null,
        ?array $resourceType = null,
        ?array $resourceId = null,
        ?array $actorId = null,
        ?LogOrdering $order = null,
        ?int $limit = null,
    ): Paginator {
        return new Paginator(fn(int|string|null $position): Page => $this->list(
            date: $date,
            product: $product,
            resourceType: $resourceType,
            resourceId: $resourceId,
            actorId: $actorId,
            order: $order,
            continuationToken: \is_string($position) ? $position : null,
            limit: $limit,
        ));
    }
}
