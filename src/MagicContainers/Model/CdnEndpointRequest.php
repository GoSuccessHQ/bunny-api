<?php

declare(strict_types=1);

// This file is generated by tools/generate.php from resources/specs/magic-containers.json. Do not edit.

namespace GoSuccess\Bunny\MagicContainers\Model;

use GoSuccess\Bunny\Model\Cast;
use GoSuccess\Bunny\Model\RequestModel;
use GoSuccess\Bunny\Model\ResponseModel;
use GoSuccess\Bunny\Model\Undefined;

/** Schema: CdnEndpointRequest */
final readonly class CdnEndpointRequest implements RequestModel, ResponseModel
{
    public bool $isSslEnabled;

    public StickySessionSettings $stickySessions;

    public ?int $pullZoneId;

    /** @var list<ContainerPortMappingRequest> */
    public array $portMappings;

    /**
     * Payload keys the caller provided; toArray() sends exactly these.
     *
     * @var array<string, true>
     */
    private array $provided;

    /** @param list<ContainerPortMappingRequest>|Undefined $portMappings */
    public function __construct(
        bool|Undefined $isSslEnabled = Undefined::Value,
        StickySessionSettings|Undefined $stickySessions = Undefined::Value,
        int|Undefined|null $pullZoneId = Undefined::Value,
        array|Undefined $portMappings = Undefined::Value,
    ) {
        $this->isSslEnabled = $isSslEnabled instanceof Undefined ? false : $isSslEnabled;
        $this->stickySessions = $stickySessions instanceof Undefined ? new StickySessionSettings() : $stickySessions;
        $this->pullZoneId = $pullZoneId instanceof Undefined ? null : $pullZoneId;
        $this->portMappings = $portMappings instanceof Undefined ? [] : $portMappings;
        $this->provided = array_filter([
            'isSslEnabled' => !$isSslEnabled instanceof Undefined,
            'stickySessions' => !$stickySessions instanceof Undefined,
            'pullZoneId' => !$pullZoneId instanceof Undefined,
            'portMappings' => !$portMappings instanceof Undefined,
        ]);
    }

    public static function fromArray(array $data): static
    {
        return new self(
            isSslEnabled: Cast::bool($data['isSslEnabled'] ?? null) ?? Undefined::Value,
            stickySessions: Cast::model(StickySessionSettings::class, $data['stickySessions'] ?? null) ?? Undefined::Value,
            pullZoneId: Cast::int($data['pullZoneId'] ?? null) ?? Undefined::Value,
            portMappings: Cast::modelList(ContainerPortMappingRequest::class, $data['portMappings'] ?? null) ?: Undefined::Value,
        );
    }

    public function toArray(): array
    {
        $data = [];

        if (isset($this->provided['isSslEnabled'])) {
            $data['isSslEnabled'] = $this->isSslEnabled;
        }

        if (isset($this->provided['stickySessions'])) {
            $data['stickySessions'] = $this->stickySessions->toArray();
        }

        if (isset($this->provided['pullZoneId'])) {
            $data['pullZoneId'] = $this->pullZoneId;
        }

        if (isset($this->provided['portMappings'])) {
            $data['portMappings'] = array_map(static fn(ContainerPortMappingRequest $item): array => $item->toArray(), $this->portMappings);
        }

        return $data;
    }
}
