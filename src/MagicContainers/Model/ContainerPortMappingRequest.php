<?php

declare(strict_types=1);

// This file is generated by tools/generate.php from resources/specs/magic-containers.json. Do not edit.

namespace GoSuccess\Bunny\MagicContainers\Model;

use GoSuccess\Bunny\MagicContainers\Enum\Protocol;
use GoSuccess\Bunny\Model\Cast;
use GoSuccess\Bunny\Model\RequestModel;
use GoSuccess\Bunny\Model\ResponseModel;
use GoSuccess\Bunny\Model\Undefined;

/** Schema: ContainerPortMappingRequest */
final readonly class ContainerPortMappingRequest implements RequestModel, ResponseModel
{
    public int $containerPort;

    public ?int $exposedPort;

    /** @var list<Protocol> */
    public array $protocols;

    /**
     * Payload keys the caller provided; toArray() sends exactly these.
     *
     * @var array<string, true>
     */
    private array $provided;

    /** @param list<Protocol>|Undefined $protocols */
    public function __construct(
        int|Undefined $containerPort = Undefined::Value,
        int|Undefined|null $exposedPort = Undefined::Value,
        array|Undefined $protocols = Undefined::Value,
    ) {
        $this->containerPort = $containerPort instanceof Undefined ? 0 : $containerPort;
        $this->exposedPort = $exposedPort instanceof Undefined ? null : $exposedPort;
        $this->protocols = $protocols instanceof Undefined ? [] : $protocols;
        $this->provided = array_filter([
            'containerPort' => !$containerPort instanceof Undefined,
            'exposedPort' => !$exposedPort instanceof Undefined,
            'protocols' => !$protocols instanceof Undefined,
        ]);
    }

    public static function fromArray(array $data): static
    {
        return new self(
            containerPort: Cast::int($data['containerPort'] ?? null) ?? Undefined::Value,
            exposedPort: Cast::int($data['exposedPort'] ?? null) ?? Undefined::Value,
            protocols: Cast::listOf($data['protocols'] ?? null, static fn(mixed $value): ?Protocol => Cast::stringEnum(Protocol::class, $value)) ?: Undefined::Value,
        );
    }

    public function toArray(): array
    {
        $data = [];

        if (isset($this->provided['containerPort'])) {
            $data['containerPort'] = $this->containerPort;
        }

        if (isset($this->provided['exposedPort'])) {
            $data['exposedPort'] = $this->exposedPort;
        }

        if (isset($this->provided['protocols'])) {
            $data['protocols'] = array_map(static fn(Protocol $item): string => $item->value, $this->protocols);
        }

        return $data;
    }
}
