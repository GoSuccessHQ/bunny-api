<?php

declare(strict_types=1);

// This file is generated by tools/generate.php from resources/specs/edge-scripting.json. Do not edit.

namespace GoSuccess\Bunny\EdgeScripting;

use GoSuccess\Bunny\ClientOptions;
use GoSuccess\Bunny\EdgeScripting\Resource\EdgeScriptResource;
use GoSuccess\Bunny\EdgeScripting\Resource\ReleaseResource;
use GoSuccess\Bunny\EdgeScripting\Resource\SecretResource;
use GoSuccess\Bunny\EdgeScripting\Resource\VariableResource;
use GoSuccess\Bunny\Http\Connection;
use GoSuccess\Bunny\Http\CurlHttpClient;
use GoSuccess\Bunny\Http\HttpClient;
use GoSuccess\Bunny\RateLimit\NullRateLimiter;
use GoSuccess\Bunny\RateLimit\RateLimiter;
use SensitiveParameter;

/**
 * Client for the bunny.net Edge Scripting API (`https://api.bunny.net`).
 *
 * Resources are created on first access, so unused ones cost nothing.
 */
final class EdgeScriptingClient
{
    public const string DEFAULT_BASE_URI = 'https://api.bunny.net';

    /** Edge scripts: their settings, code, statistics and deployment key. */
    public private(set) EdgeScriptResource $scripts {
        get => $this->scripts ??= new EdgeScriptResource($this->connection);
    }

    /** Releases of edge scripts: the published versions of their code. */
    public private(set) ReleaseResource $releases {
        get => $this->releases ??= new ReleaseResource($this->connection);
    }

    /** Environment variables of edge scripts. */
    public private(set) VariableResource $variables {
        get => $this->variables ??= new VariableResource($this->connection);
    }

    /** Secrets of edge scripts; their values can be written, never read. */
    public private(set) SecretResource $secrets {
        get => $this->secrets ??= new SecretResource($this->connection);
    }

    private readonly Connection $connection;

    /**
     * @param string          $apiKey      The account API key (bunny.net dashboard → Account settings → API key).
     * @param ClientOptions   $options     Timeouts, retries and user agent.
     * @param HttpClient|null $httpClient  Custom transport; defaults to the built-in cURL transport.
     * @param RateLimiter     $rateLimiter Client-side throttling; disabled by default.
     * @param string          $baseUri     Base URI of the API.
     */
    public function __construct(
        #[SensitiveParameter]
        string $apiKey,
        ClientOptions $options = new ClientOptions(),
        ?HttpClient $httpClient = null,
        RateLimiter $rateLimiter = new NullRateLimiter(),
        string $baseUri = self::DEFAULT_BASE_URI,
    ) {
        $this->connection = new Connection(
            $baseUri,
            $apiKey,
            $options,
            $httpClient ?? new CurlHttpClient($options->timeout, $options->connectTimeout),
            $rateLimiter,
        );
    }
}
