<?php

declare(strict_types=1);

// This file is generated by tools/generate.php from resources/specs/stream.json. Do not edit.

namespace GoSuccess\Bunny\Stream\Model;

use GoSuccess\Bunny\Model\Cast;
use GoSuccess\Bunny\Model\RequestModel;
use GoSuccess\Bunny\Model\ResponseModel;
use GoSuccess\Bunny\Model\Undefined;

/** Schema: MomentModel */
final readonly class Moment implements RequestModel, ResponseModel
{
    /** The text description label for the moment, up to 255 characters. */
    public string $label;

    /** The timestamp of the moment in seconds. Must be within the video's duration. */
    public int $timestamp;

    /**
     * Payload keys the caller provided; toArray() sends exactly these.
     *
     * @var array<string, true>
     */
    private array $provided;

    public function __construct(
        string|Undefined $label = Undefined::Value,
        int|Undefined $timestamp = Undefined::Value,
    ) {
        $this->label = $label instanceof Undefined ? '' : $label;
        $this->timestamp = $timestamp instanceof Undefined ? 0 : $timestamp;
        $this->provided = array_filter([
            'label' => !$label instanceof Undefined,
            'timestamp' => !$timestamp instanceof Undefined,
        ]);
    }

    public static function fromArray(array $data): static
    {
        return new self(
            label: Cast::string($data['label'] ?? null) ?? Undefined::Value,
            timestamp: Cast::int($data['timestamp'] ?? null) ?? Undefined::Value,
        );
    }

    public function toArray(): array
    {
        $data = [];

        if (isset($this->provided['label'])) {
            $data['label'] = $this->label;
        }

        if (isset($this->provided['timestamp'])) {
            $data['timestamp'] = $this->timestamp;
        }

        return $data;
    }
}
